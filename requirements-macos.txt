# macOS CPU environment. Python 3.11 is recommended.
# PyTorch automatically uses CPU on an Intel Mac and may use MPS where the
# installed PyTorch build and hardware support it; CUDA is not required.
torch==2.0.1
torchvision==0.15.2
segmentation-models-pytorch==0.5.0
numpy==1.24.4
Pillow==11.3.0
scipy==1.10.0
matplotlib==3.10.3
spectral==0.24
pytest==9.1.0

